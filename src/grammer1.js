let string = [];
if (string.length === 0) {
  console.log(`${string.length} : 비어있는 배열이다`);
} else {
  console.log(`${string.length} : 비어 있지 않음.`);
}

let text = string === 0 ? "비어있다" : "비어있지 않음";
console.log(text);
